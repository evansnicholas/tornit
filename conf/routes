# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET /entrypoints                    controllers.Application.listEntrypoints

GET /dtsGraph/*entrypointPath       controllers.Application.computeDtsGraph(entrypointPath)

GET /presentationViewer             controllers.Application.presentationViewer

GET /dimensionViewer                controllers.Application.dimensionViewer

GET /dtsGraph                       controllers.Application.dtsGraph
