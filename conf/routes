# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.main

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET /entrypoints                    controllers.Application.listEntrypoints(uri)

GET /dtsGraph                       controllers.Application.computeDtsGraph(uri)

GET /concepts                       controllers.Application.listConcepts(uri, concept)

GET /dimensionsGraph                controllers.Application.computeDimensionalGraphs(uri, namespace, localPart)
